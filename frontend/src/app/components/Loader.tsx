// Loader.js
import React from 'react';
import styles from'./styles/loader.module.css'; // Make sure to import the CSS

const Loader = () => {
  return (
    <div className={styles.loader}></div>
  );
};

export default Loader;
